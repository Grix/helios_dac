This makefile allows build of libboard_sam4s_ek_???.a

where 
??? could be dbg and rel (debug, release)

It checks for source files (C and assembler) from folder ../../source then compiles them to build given library
