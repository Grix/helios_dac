This makefile allows build of libchip_*_???.a

where 
* could be sam4s4, sam4s8, sam4s16
??? could be dbg and rel (debug, release)

It checks for source files (C and assembler) from folders ../../source then compiles them to build given library
